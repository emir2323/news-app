plugins {
    id("com.android.application")
    id("org.jetbrains.kotlin.android")
    id("dev.flutter.flutter-gradle-plugin")
    id("com.google.gms.google-services") // Google Services Plugin doğru şekilde eklenmiş
}

android {
    namespace = "com.example.news_app"
    compileSdk = flutter.compileSdkVersion
    ndkVersion = "27.0.12077973"  // Bu satır Flutter'ın gerektirdiği sürüm olmalı

    compileOptions {
        sourceCompatibility JavaVersion.VERSION_17
        targetCompatibility JavaVersion.VERSION_17
        coreLibraryDesugaringEnabled true
    }

    kotlinOptions {
        jvmTarget = '17'
    }

    defaultConfig {
        applicationId = "com.example.news_app"
        minSdk = 21
        targetSdk = flutter.targetSdkVersion
        versionCode = 1
        versionName = "1.0.0"
        multiDexEnabled = true
    }

    buildTypes {
        release {
            signingConfig = signingConfigs.debug  // Bu satır release yaparken debug key ile imzalama yapıyor
        }
    }
}

flutter {
    source = "../.."  // Flutter projene doğru kaynak yolu verilmeli
}

dependencies {
    implementation(platform("com.google.firebase:firebase-bom:32.7.0"))  // Firebase BOM kullanarak versiyon uyumunu sağlıyoruz
    implementation("com.google.firebase:firebase-analytics")
    implementation("com.google.firebase:firebase-auth")
    implementation("com.android.support:multidex:1.0.3")
    implementation("com.google.android.gms:play-services-safetynet:18.0.1")
    coreLibraryDesugaring 'com.android.tools:desugar_jdk_libs:2.0.4'
}
